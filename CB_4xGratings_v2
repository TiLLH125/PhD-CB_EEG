Please modify the latest CB_4xGratings_v2.m script with two targeted timing-polish changes only.

Do not change QUEST+, trial logic, PAS/localisation scoring, response handling, trial randomisation, stimulus layout, cached question textures, the post-S2 gap fix, or the flip logging system except where needed to log the modified flips.

Goal:
Reduce the small 3–5 ms timing slips on non-critical immediate flips by:
1. Calling Screen('DrawingFinished', window) before selected flips.
2. Scheduling those flips slightly into the future using GetSecs + 0.5 * ifi.

Important context:
The critical stimulus sequence is now okay. The remaining larger small slips are mostly on non-critical or response-related flips such as FixOn, Q2_Loc, ITI, and practice feedback. We are not trying to change the experiment logic; this is only a timing polish.

Required change 1: Main trial FixOn

Find the main trial FixOn section:

drawFixationOnly(window, bg, fixationCoords, cfg.fix.lineWidthPx, black, xCentre, yCentre);
[tFixOn, ~, ~, missedFixOn] = cfg.loggedFlip('FixOn', 'main_trial', t, ceil(t / cfg.trialsPerBlock), NaN);

Replace it with:

drawFixationOnly(window, bg, fixationCoords, cfg.fix.lineWidthPx, black, xCentre, yCentre);
Screen('DrawingFinished', window);
vblTargetFixOn = GetSecs + 0.5 * ifi;
[tFixOn, ~, ~, missedFixOn] = cfg.loggedFlip('FixOn', 'main_trial', t, ceil(t / cfg.trialsPerBlock), vblTargetFixOn);

Do not change how S1 is scheduled from tFixOn.

Required change 2: Main trial Q2_Loc

Find the main trial Q2 section:

if ~isnan(pas) && pas == 1
    Screen('DrawTexture', window, cfg.qTex.Loc_pas1);
else
    Screen('DrawTexture', window, cfg.qTex.Loc_default);
end
[tQ2, ~, ~, missedQ2] = cfg.loggedFlip('Q2_Loc', 'main_trial', t, ceil(t / cfg.trialsPerBlock), NaN);

Replace it with:

if ~isnan(pas) && pas == 1
    Screen('DrawTexture', window, cfg.qTex.Loc_pas1);
else
    Screen('DrawTexture', window, cfg.qTex.Loc_default);
end
Screen('DrawingFinished', window);
vblTargetQ2 = GetSecs + 0.5 * ifi;
[tQ2, ~, ~, missedQ2] = cfg.loggedFlip('Q2_Loc', 'main_trial', t, ceil(t / cfg.trialsPerBlock), vblTargetQ2);

Do not change PAS logic or localisation logic.

Required change 3: Main trial ITI

Find the main trial ITI section:

Screen('FillRect', window, bg);
[tITI, ~, ~, missedITI] = cfg.loggedFlip('ITI', 'main_trial', t, ceil(t / cfg.trialsPerBlock), NaN);

Replace it with:

Screen('FillRect', window, bg);
Screen('DrawingFinished', window);
vblTargetITI = GetSecs + 0.5 * ifi;
[tITI, ~, ~, missedITI] = cfg.loggedFlip('ITI', 'main_trial', t, ceil(t / cfg.trialsPerBlock), vblTargetITI);

Do not change the subsequent ITI hold duration logic.

Required change 4: Practice trial FixOn

In runPracticeBlock, find the equivalent practice FixOn flip. Apply the same pattern:

drawFixationOnly(...);
Screen('DrawingFinished', window);
vblTargetFixOn = GetSecs + 0.5 * ifi;
[tFixOn, ~, ~, missedFixOn] = cfg.loggedFlip('FixOn', pracCfg.name, p, NaN, vblTargetFixOn);

Keep the practice S1 scheduling based on the returned tFixOn.

Required change 5: Practice trial Q2_Loc

In runPracticeBlock, find the equivalent practice Q2 localisation flip. Apply:

Screen('DrawingFinished', window);
vblTargetQ2 = GetSecs + 0.5 * ifi;
[tQ2, ~, ~, missedQ2] = cfg.loggedFlip('Q2_Loc', pracCfg.name, p, NaN, vblTargetQ2);

Do not change the response/scoring logic.

Required change 6: Practice trial ITI

In runPracticeBlock, find the equivalent practice ITI flip. Apply:

Screen('FillRect', window, bg);
Screen('DrawingFinished', window);
vblTargetITI = GetSecs + 0.5 * ifi;
[tITI, ~, ~, missedITI] = cfg.loggedFlip('ITI', pracCfg.name, p, NaN, vblTargetITI);

Required change 7: Practice feedback

Find the practice feedback screen flip. Before the flip, add:

Screen('DrawingFinished', window);
vblTargetFeedback = GetSecs + 0.5 * ifi;

Then route it through cfg.loggedFlip with a scheduled when argument:

cfg.loggedFlip('practice_feedback', pracCfg.name, p, NaN, vblTargetFeedback);

If the current practice feedback function does not have p or pracCfg.name available, use whatever equivalent practice trial index/context variables are already available. Do not restructure practice logic unless necessary.

Important:
- Do not add any direct Screen('Flip', window) calls outside loggedFlip.
- Keep all visible flips routed through cfg.loggedFlip.
- Keep Q1_PAS scheduled from tGap + cfg.gap_frames exactly as currently implemented.
- Keep S1, ISI, S2, and Gap timing exactly as currently implemented.
- Keep Priority(MaxPriority(window)) and cleanup Priority(0).
- Keep the flip log summary and actual frame-duration diagnostics.

After implementing, briefly report:
1. Which flips were changed from immediate to scheduled.
2. Whether every visible flip still routes through cfg.loggedFlip.
3. Whether Q1_PAS is still scheduled from the post-S2 gap.
4. Whether S1/ISI/S2/Gap timing was left unchanged.
